import React from 'react';
import { Card, CardTitle } from 'material-ui/Card';

const ToDoPage = () => (
  <Card className="container">
    <CardTitle title="Web Tracker" subtitle="The page you visited is under construction." />
  </Card>
);

export default ToDoPage;
